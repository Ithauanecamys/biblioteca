<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Usuários</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <div class="container">
    <div class="card">
      <h2>Usuários Cadastrados</h2>
      <p style="color:#777; margin-bottom:20px;">Você só pode ver os nomes dos usuários.</p>

      <table class="table">
        <thead>
          <tr>
            <th>ID</th>
            <th>Nome</th>
          </tr>
        </thead>
        <tbody>
          <% if (usuarios.length === 0) { %>
            <tr><td colspan="2" style="text-align:center; color:#999;">Nenhum usuário.</td></tr>
          <% } else { %>
            <% usuarios.forEach(u => { %>
              <tr>
                <td><%= u.id %></td>
                <td><%= u.nome %></td>
              </tr>
            <% }) %>
          <% } %>
        </tbody>
      </table>

      <a href="/dashboard" class="voltar">Voltar</a>
    </div>
  </div>
</body>
</html>
