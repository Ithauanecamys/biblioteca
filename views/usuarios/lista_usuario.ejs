<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Usuários</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <div class="container">
    <div class="card">
      <h2>Usuários Cadastrados</h2>
      <p style="color:#777; margin-bottom:20px; font-size:15px; text-align:center;">
        Você pode visualizar apenas os nomes dos usuários.
      </p>

      <table class="table">
        <thead>
          <tr>
            <th>ID</th>
            <th>Nome</th>
          </tr>
        </thead>
        <tbody>
          <% if (usuarios.length === 0) { %>
            <tr>
              <td colspan="2" style="text-align:center; color:#999; padding:25px;">
                Nenhum usuário cadastrado.
              </td>
            </tr>
          <% } else { %>
            <% usuarios.forEach(u => { %>
              <tr>
                <td><strong>#<%= u.id %></strong></td>
                <td><%= u.nome %></td>
              </tr>
            <% }) %>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>

  <!-- Seta fixa no canto inferior esquerdo (sem texto) -->
  <a href="/dashboard" class="back-arrow" title="Voltar ao Dashboard"></a>
</body>
</html>
